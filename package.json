{
  "dependencies": {
    "sass": "^1.69.5"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "node-sass": "^9.0.0"
  },

  "scripts": {
    "start": "live-server",
    "sass:watch": "node-sass --watch scss/ -o css/ --output-style compressed",
    "dev": "npm run sass:watch & npm start"
  }
}
